<!-- <div class="trip-post-container">
  <div class="trip-post-content-container">
    <div class="trip-title-container">
      <h2 class="trip-post-tite">Trip to {{location}}</h2
    </div>
    <div class="trip-dates-container">
      <h2 class="trip-dates">Start date: {{startDate}} End date: {{endDate}}</h2>
    </div>
    <div class="trip-time-container">
      <h2 class="trip length">Trip length: {{time}} days</h2>
    </div>
    <div class="trip-photo-container">
      <img src="{{tripPhoto}}">
    </div>
    <div class="trip-notes-title-container">
      <h2 class="notes-title">Notes:</h2>
    </div>
    <div class="notes-container">
      <p class="notes">{{notes}}</p>
    </div>
  </div>
</div> -->

<div class="trip" data-title={{tripTitle}}>
  <div class="trip-contents">
    <div class="trip-image-container">
      <img src={{tripPostImage}}>
    </div>
    <div class="trip-info-container">
      <button type="button" id="0" class="trip-title">{{tripTitle}}</button>
      <div class="trip-date">{{tripStartDate}} - {{tripEndDate}}</div>
      <div class="trip-location">{{location}}</div>
      <!-- Whats this for? -->
      <p>This is a place</p>
    </div>
  </div>
</div>
